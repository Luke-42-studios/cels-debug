# Summary: 03.1-01 — Create tab_ecs (merged entity+component), update wiring to 4-tab layout

## Status: Complete

## What Was Built

Restructured the debugger from 5 tabs to 4 tabs (Overview, ECS, Performance, State), merging entity and component browsing into a unified ECS tab with a context-sensitive inspector panel.

## Deliverables

| File | What Changed |
|------|-------------|
| `src/tabs/tab_ecs.h` | New header declaring tab_ecs_init/fini/draw/input |
| `src/tabs/tab_ecs.c` | Merged tab_entities + tab_components: CELS-C tree view with classification, annotation from component registry, context-sensitive inspector (component type → entities-with-component, entity → component values) |
| `src/tab_system.h` | TAB_COUNT 5 → 4 |
| `src/tab_system.c` | 4-entry tab_defs array referencing tab_ecs functions, removed tab_entities include |
| `src/main.c` | Key range derives from TAB_COUNT instead of hardcoded '5' |
| `src/tui.c` | Footer updated to "1-4:tabs" |
| `CMakeLists.txt` | Builds tab_ecs.c instead of tab_entities.c |

## Commits

| # | Hash | Message |
|---|------|---------|
| 1 | f9ca26e | feat(03.1-01): create tab_ecs.h/c with merged entity+component logic |
| 2 | 09ba6b7 | feat(03.1-01): wire 4-tab layout (Overview, ECS, Performance, State) |

## Deviations

None.

## Issues

None. Build compiles cleanly with zero errors.
